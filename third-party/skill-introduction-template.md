# Skill 介绍模板

> 将本文中的占位符替换为目标 skill 的真实信息。建议保留章节结构，删去不适用的小节。

## 基础信息

| 项目 | 内容 |
| --- | --- |
| Skill 名称 | `<skill-name>` |
| 显示名称 | `<Skill Display Name>` |
| 所属分类 | `<如：开发工具 / 文档处理 / 数据分析 / 浏览器自动化>` |
| 适用平台 | `<如：Codex / Claude Code / Gemini CLI / 其他>` |
| 当前版本 | `<版本号或提交哈希>` |
| 维护者 | `<作者、团队或仓库地址>` |
| 来源 | `<官方 / 第三方 / 自研 / 开源项目>` |
| GitHub 仓库 | `<https://github.com/owner/repo，若无则填写“不适用”>` |
| 其他来源链接 | `<非 GitHub 来源、发布页、压缩包下载页或插件市场链接>` |
| 许可证 | `<MIT / Apache-2.0 / Proprietary / 未知>` |
| 更新时间 | `<YYYY-MM-DD>` |

## 简介

`<skill-name>` 是一个用于 `<一句话说明核心用途>` 的 skill。它适合在 `<典型场景>` 中使用，可以帮助 Codex `<说明它能让 Codex 更擅长完成什么任务>`。

## 适用场景

- `<场景 1：用户会在什么情况下调用它>`
- `<场景 2：它适合处理哪类文件、工具、平台或流程>`
- `<场景 3：它能减少哪些重复操作或提升哪些能力>`

## 不适用场景

- `<不适合的任务类型 1>`
- `<不应由该 skill 处理的边界 2>`
- `<需要人工确认或额外权限的场景 3>`

## 详细功能介绍

### 核心能力

- **`<能力 1>`**：`<说明该能力解决的问题、输入、输出和关键限制>`。
- **`<能力 2>`**：`<说明该能力的工作方式和典型收益>`。
- **`<能力 3>`**：`<说明该能力与普通模型能力相比的增强点>`。

### 工作流程

1. `<步骤 1：skill 触发后通常先做什么>`
2. `<步骤 2：如何读取上下文、选择工具或加载资源>`
3. `<步骤 3：如何执行任务并处理中间结果>`
4. `<步骤 4：如何验证结果或交付产物>`

### 内置资源

| 资源类型 | 路径 | 用途 |
| --- | --- | --- |
| `SKILL.md` | `<path/to/SKILL.md>` | `<核心说明、触发条件和执行流程>` |
| `scripts/` | `<path/to/scripts>` | `<自动化脚本、校验脚本或生成脚本>` |
| `references/` | `<path/to/references>` | `<按需加载的文档、规范、示例或 API 说明>` |
| `assets/` | `<path/to/assets>` | `<模板、图片、字体、示例工程等输出资源>` |

### 输入与输出

| 类型 | 说明 |
| --- | --- |
| 输入 | `<用户需要提供的问题、文件、链接、配置或权限>` |
| 输出 | `<skill 通常生成的文档、代码、报告、文件或操作结果>` |
| 副作用 | `<是否会修改文件、启动服务、访问网络、安装依赖等>` |

## 安装方法

### 前置条件

- `<运行环境要求，如 Python、Node.js、uv、特定 CLI 或 API key>`
- `<平台要求，如 Windows、macOS、Linux 或 Codex 插件环境>`
- `<权限要求，如文件写入、网络访问、浏览器控制等>`

### 安装步骤

1. 确认来源地址：

   ```text
   GitHub 仓库：<https://github.com/owner/repo>
   其他来源：<https://example.com/path/to/skill>
   ```

2. 获取 skill：

   ```bash
   <git clone GitHub 仓库，或从其他来源下载解压>
   ```

3. 放置到 Codex skills 目录：

   ```bash
   <复制或安装命令>
   ```

4. 确认目录结构：

   ```text
   <skill-name>/
   |-- SKILL.md
   |-- agents/
   |   `-- openai.yaml
   |-- scripts/
   |-- references/
   `-- assets/
   ```

5. 重启或刷新 Codex，使 skill 元数据重新加载。

### 安装验证

运行以下检查，确认 skill 可被发现且关键文件存在：

```bash
<验证命令，如 scripts/quick_validate.py path/to/skill 或列出已安装 skills>
```

预期结果：

```text
<示例输出或成功标志>
```

## 使用方法

### 触发方式

用户可以通过以下方式触发该 skill：

- 明确点名：`使用 <skill-name> ...`
- 描述任务：`<写一个会自然触发该 skill 的用户请求示例>`
- 提供相关文件：`<如上传 .docx、.xlsx、PDF、代码仓库等>`

### 使用示例

#### 示例 1：`<常见任务名称>`

用户请求：

```text
<示例用户提示词>
```

预期行为：

```text
<Codex 应该如何使用该 skill，以及最终会交付什么>
```

#### 示例 2：`<进阶任务名称>`

用户请求：

```text
<示例用户提示词>
```

预期行为：

```text
<Codex 应该加载哪些资源、执行哪些步骤、如何验证>
```

## 配置说明

| 配置项 | 是否必需 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `<CONFIG_NAME>` | `<是/否>` | `<默认值>` | `<用途和设置方式>` |
| `<API_KEY>` | `<是/否>` | `<无>` | `<相关服务、权限范围和安全注意事项>` |

## 常见问题

### `<问题 1>`

`<回答：原因、解决方法、排查命令或注意事项>`。

### `<问题 2>`

`<回答：兼容性、权限、网络或依赖相关说明>`。

## 风险与限制

- `<限制 1：模型可能需要额外上下文或人工确认>`
- `<限制 2：外部服务、网络、权限或依赖可能导致失败>`
- `<限制 3：对文件格式、版本、平台或数据规模的限制>`

## 维护与更新

- 更新 `SKILL.md` 时，同步检查 frontmatter 中的 `name` 和 `description` 是否准确。
- 如果修改了 `scripts/`，至少运行一个代表性示例验证脚本行为。
- 如果修改了 `references/`，确认 `SKILL.md` 中仍然清楚说明何时读取对应文档。
- 如果修改了 UI 元数据，确认 `agents/openai.yaml` 与 skill 的真实能力一致。

## 参考链接

- GitHub 仓库：`<https://github.com/owner/repo>`
- 其他来源：`<非 GitHub 仓库、插件市场、发布页或下载页>`
- 官方文档：`<相关官方文档链接>`
- 使用示例：`<相关教程、示例或 API 文档>`
